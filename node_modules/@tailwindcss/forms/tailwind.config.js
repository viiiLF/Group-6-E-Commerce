module.exports = {
  content: ['./index.html', './kitchen-sink.html'],
  theme: {
    extend: {

    },
  },
  plugins: [require('./src')],
}

module.exports = {
  theme: {
    extend: {
      fontFamily: {
        leckerli: ['Leckerli One', 'cursive'],
      },
    },
  },
  plugins: [],
};
module.exports = {
  theme: {
    extend: {
      fontFamily: {
        lemon: ['Lemon', 'serif'],
      },
    },
  },
  plugins: [],
};

module.exports = {
  theme: {
    extend: {
      textShadow: {
        'green': '2px 2px 4px rgba(0, 128, 0, 0.8)',
        'green-thick': '4px 4px 8px rgba(0, 128, 0, 0.8)',
      },
    },
  },
  plugins: [
    function ({ addUtilities, theme }) {
      const textShadowUtilities = {};
      Object.entries(theme('textShadow')).forEach(([key, value]) => {
        textShadowUtilities[`.text-shadow-${key}`] = {
          'text-shadow': value,
        };
      });
      addUtilities(textShadowUtilities);
    },
  ],
};

